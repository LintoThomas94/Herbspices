<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <style>
        body {
            background-color: #f0f2f5;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        /* Main content wrapper */
    .main-content { padding: 8px; margin-left: 220px; }

    /* Header */
    .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; }
    .page-title { font-size: 26px; font-weight: bold; color: #2c3e50; }

    /* Search */
    .search-container {
      display: flex; align-items: center; background: white;
      border-radius: 30px; padding: 5px 15px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .search-input { border: none; outline: none; padding: 8px 12px; font-size: 14px; flex: 1; }
    .search-btn { border: none; background: #3498db; border-radius: 50%; padding: 8px; cursor: pointer; display: flex; justify-content: center; align-items: center; }
    .search-btn:hover { background: #2980b9; }
    .search-btn svg { fill: #ffffff; }

        
        /* Style the form card */
        .card {
            border-radius: 15px;
            border: none;
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }
        
        /* Style the form inputs and textarea */
        .styled-input {
            border: 1px solid #ced4da;
            border-radius: 10px;
            padding: 10px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }
        
        .styled-input:focus {
            border-color: #007bff;
            box-shadow: 0 0 0 0.25rem rgba(0, 123, 255, 0.25);
            outline: none;
        }
        
        /* Style the update button */
        .btn-primary {
            background-color: #007bff;
            border: none;
            font-weight: bold;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }
        
        .btn-primary:hover {
            background-color: #0056b3;
            transform: scale(1.02);
        }
        
        /* Style for the card title */
        .card-title {
            color: #343a40;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.05);
        }
    </style>
</head>
<body>
    <%- include("../partials/admin/header") %>
    <main class="main-content">
    <!-- Header -->
    <header class="header">
      <h1 class="page-title">Edit-Category</h1>
      <div class="search-container">
        <input type="text" class="search-input" placeholder="Search ...">
        <button class="search-btn">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24">
            <path d="M15.5 14h-.79l-.28-.27C15.41 12.59 
                     16 11.11 16 9.5 16 5.91 13.09 3 
                     9.5 3S3 5.91 3 9.5 5.91 16 
                     9.5 16c1.61 0 3.09-.59 4.23-1.57
                     l.27.28v.79l5 4.99L20.49 
                     19l-4.99-5zm-6 0C7.01 14 5 
                     11.99 5 9.5S7.01 5 9.5 5 
                     14 7.01 14 9.5 11.99 14 
                     9.5 14z"/>
          </svg>
        </button>
      </div>
    </header>

<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="col-12 col-md-8 col-lg-6">
        <div class="card shadow-lg p-4 my-5 bg-white">
            <div class="card-body">
                <h3 class="card-title text-center mb-4 text-dark fw-bold">Edit Category</h3>
                
                <form method="post" action="/admin/editCategory/<%=category._id%>">
                    <div class="mb-4">
                        <label for="category_name" class="form-label text-dark fw-bold">Name</label>
                        <input type="text" name="categoryname" value="<%= category.name %>" class="form-control border styled-input" id="category_name" required/>
                    </div>
                    
                    <div class="mb-4">
                        <label for="category_description" class="form-label text-dark fw-bold">Description</label>
                        <textarea name="description" class="form-control border styled-input" id="category_description" rows="4"><%= category.description %></textarea>
                    </div>

                    <div class="d-grid gap-2 mt-4">
                        <button class="btn btn-primary btn-lg rounded-pill" type="submit">Update Category</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

</body>
</html>